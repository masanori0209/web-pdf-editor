version: '3'
services:
  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    command: 'sh /app/start-front.sh'
    volumes:
      - ./app:/app
      - ./wasm:/wasm
    ports:
      - 8080:8080
  wasm:
    build:
      context: ./wasm
      dockerfile: Dockerfile
    volumes:
      - ./wasm:/wasm
